// The bare lab: the 3D world and its HUD live in the layout, which persists
// across /lab/* navigation. This route intentionally renders nothing itself.
export default function LabPage() {
  return null;
}
