# Pinned to what is installed and verified in the `retrosynth` env.
# aizynthfinder pulls rdkit, onnxruntime, numpy, pandas, networkx, tables itself.
aizynthfinder==4.4.1
fastapi==0.141.1
uvicorn[standard]==0.52.4
pytest==9.1.1
httpx==0.28.1
psycopg[binary,pool]==3.2.10
scikit-learn==1.5.2
xgboost==2.1.3
# NeoChems agent orchestration. Pulls langchain-core; installing it moves
# websockets 17 -> 16, which uvicorn accepts.
langgraph==1.2.11

# NOT here on purpose: chemprop wants rdkit>=2026 and networkx>=3, which break
# AiZynthFinder's pins and the Postgres cartridge coupling. It lives in the
# separate `qsar-chemprop` env - see backend/qsar/README.md.
